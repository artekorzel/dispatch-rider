package algorithm;


/**
 * TIME - by time, shorter times first
 * DISTANCE - by distance, lower distance first
 * WORST_DISTANCE_FIRST - the worst distance first, then it's same as in distance
 */
public enum Brute2Sorter {
    TIME,
    DISTANCE,
    WORST_DISTANCE_FIRST,
    NONE
}
